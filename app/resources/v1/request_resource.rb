# frozen_string_literal: true

module V1
  #
  # @note This endpoint can't be directly accessed via the `/v1/requests/` endpoint
  # as it is not currently used.
  #
  class RequestResource < JSONAPI::Resource
  end
end
