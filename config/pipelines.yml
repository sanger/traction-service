default: &default
  saphyr:
    messages:
      lims: Traction
      instrument_name: saphyr
      key: bmap_flowcell
      fields:
        sample_uuid: library.sample.external_id
        study_uuid: library.sample.external_study_id
        experiment_name: chip.run.name
        enzyme_name: library.enzyme.name
        chip_barcode: chip.barcode
        chip_serialnumber: chip.serial_number
        position: position
        id_library_lims: library.id
        id_flowcell_lims: id

development:
  <<: *default

test:
  <<: *default
